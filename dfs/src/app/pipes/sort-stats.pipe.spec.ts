import { SortStatsPipe } from './sort-stats.pipe';

describe('SortStatsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortStatsPipe();
    expect(pipe).toBeTruthy();
  });
});
